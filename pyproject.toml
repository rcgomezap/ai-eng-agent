[project]
name = "ai-eng-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.13.4",
]

[tool.pyright]
typeCheckingMode = "strict"

[dependency-groups]
dev = [
    "ruff>=0.16.3",
]
